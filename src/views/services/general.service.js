import { zergApi } from "@/service/zerg";

class GeneralService {
    async getModules(params) {
        try {
            return [];
        } catch (error) {
            throw error;
        }
    }
};